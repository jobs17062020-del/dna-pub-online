# ผลตรวจสอบฟีเจอร์ป้องกันข้อมูลซ้ำ

ตรวจใน preview วันที่ 26 สิงหาคม 2026 ด้วยรายการเดิมเลข 1234 ที่มีอยู่แล้วในรอบปัจจุบัน เมื่อกรอก 1234 และกดบันทึก ระบบแสดงข้อความ `บันทึกไม่ได้: เลข 1234 มีอยู่แล้วในรอบนี้` และจำนวนรายการยังคงเป็น 1/100 ไม่เพิ่มรายการใหม่

โค้ดตรวจเลขซ้ำก่อนสร้างรายการ และตรวจลายนิ้วมือภาพด้วย SHA-256 จากข้อมูลไบนารีของภาพก่อนเริ่ม OCR/ก่อนสร้างรายการ โดยใช้ `imageHash` ที่บันทึกไปกับรายการ และมี fallback คำนวณจากภาพเดิมกรณีข้อมูลเก่าไม่มี hash


สำหรับการทดสอบภาพซ้ำ ใช้ SHA-256 ของไฟล์ทดสอบที่เตรียมไว้เป็น `ac10d4ec705894735f99a22458169b09d202bb74556bb3202342ab311faeb182` และเพิ่ม fixture ชั่วคราวเลข 4321 ใน localStorage ของ preview เพื่อให้การอัปโหลดไฟล์เดียวกันต้องถูกปฏิเสธจาก imageHash ก่อน OCR


รอบอัปโหลดครั้งแรกหลังเขียน fixture โดยไม่รีโหลดหน้าไม่สะท้อน fixture เพราะ React state ถูกอ่านจาก localStorage เฉพาะตอน mount ตามปกติ จึงต้องรีโหลด preview แล้วจึงทดสอบซ้ำอีกครั้ง การทำงานจริงของผู้ใช้ไม่พบประเด็นนี้ เพราะรายการถูกเพิ่มผ่าน state และ persistence พร้อมกัน


หลังรีโหลด preview ระบบอ่านข้อมูลจาก localStorage ได้ครบและแสดงจำนวน 3/100 ตามรายการเดิม 1234, รายการภาพที่ถูกเพิ่มจากการทดสอบรอบแรก และ fixture 4321 ซึ่งเตรียมไว้ตรวจ imageHash
